#pragma once

// Copy this file to secrets.h in the same folder, then fill in your real values.
// secrets.h is ignored by Git so credentials stay local.
#define WIFI_SSID "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
